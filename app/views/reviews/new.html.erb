<h2>Rate <%= @restaurant.name %></h2>

<%= form_for([@restaurant, @review]) do |f| %>

  <%= f.label :rating %>
  <%= f.select :rating, (1..5) %>

  <%= f.label :body %>
  <%= f.text_area :body %>

  <%= f.submit %>

<% end %>
